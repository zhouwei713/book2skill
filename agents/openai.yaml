interface:
  display_name: "Book2Skill"
  short_description: "Turn long-form source methods into installable Agent Skills."
  default_prompt: "Use $book2skill to turn this source material into a source-grounded skill pack."

policy:
  allow_implicit_invocation: true
